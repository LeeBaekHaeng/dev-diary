# DAO 반환값 추가

## 2026-06-23 egovframe-template-simple-backend

브랜치
```
refactor/return-dao
```

커밋 컨벤션
```
refactor: 전체 DAO 반환값 추가 및 반환값을 실제로 활용하는 변경
```

개정이력 수정내용
```
[2026년 컨트리뷰션] DAO 반환값 추가
```

PR 내용
```

추후 아래 형태로 다시 제안했습니다.
- 대상 변경을 단일 PR로 통합
- 반환값을 실제로 활용하는 변경

### 변경 이유
- DAO 메서드 수행 결과를 호출부에서 활용할 수 있도록 반환값을 추가합니다.
- INSERT/UPDATE/DELETE 처리 건수 검증이 가능하도록 개선합니다.

### 변경 내용
- insert: 처리 결과 반환
- update: 처리 결과 반환
- delete: 처리 결과 반환

### 기대 효과
- DAO 수행 결과 검증 가능
- 테스트 코드 작성 용이성 향상
- CRUD 메서드 반환 방식 일관성 확보
- 기능 변경 없는 코드 개선

```

https://github.com/eGovFramework/egovframe-template-simple-backend/pull/153
